{
    "act-price-hide": {
        "settings": {
            "info": {
                "description": "Hides prices and cart access for unauthenticated visitors and configured customer groups. A notice is shown instead of the price and checkout access is redirected until an authorised customer logs in. Prices are additionally stripped from meta tags, JSON-LD, listing aggregations and tracking scripts."
            }
        },
        "guardStatus": {
            "checking": "Checking …",
            "recheck": "Re-check",
            "primaryTitle": "Protection active (primary channel)",
            "primaryBody": "The inline script in <head> is present. The dataLayer guard installs synchronously before any tracker runs.",
            "fallbackTitle": "Protection active (fallback channel)",
            "fallbackBody": "The inline script is missing but the JS-plugin fallback reads its config from the meta tags. Your theme likely overrides 'layout/meta.html.twig' without calling {{ parent() }}. The guard installs with a delay on DOMContentLoaded.",
            "missingTitle": "Protection NOT detected",
            "missingBody": "Neither the inline script nor the fallback meta tags were found in the storefront. Either 'Enable dataLayer price-leak guard' is off in the plugin config, or a theme/plugin overrides 'layout/meta.html.twig' and 'layout_head_meta_tags_charset' without calling parent().",
            "errorTitle": "Check failed",
            "errorBody": "The storefront could not be fetched. Verify the sales channel is reachable."
        }
    }
}
